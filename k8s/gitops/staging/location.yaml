apiVersion: v1
kind: List
items:
  - apiVersion: apps/v1
    kind: Deployment
    metadata: { name: location, namespace: yas-staging, labels: { app: location, env: staging } }
    spec:
      replicas: 1
      selector: { matchLabels: { app: location } }
      template:
        metadata: { labels: { app: location, env: staging } }
        spec:
          containers:
            - name: location
              image: khanhnv2281/yas:location-latest
              ports: [{ containerPort: 8086 }]
  - apiVersion: v1
    kind: Service
    metadata: { name: location, namespace: yas-staging, labels: { app: location, env: staging } }
    spec:
      selector: { app: location }
      ports: [{ name: http, port: 80, targetPort: 8086 }]
