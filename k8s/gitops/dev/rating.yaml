apiVersion: v1
kind: List
items:
  - apiVersion: apps/v1
    kind: Deployment
    metadata: { name: rating, namespace: yas-dev, labels: { app: rating, env: dev } }
    spec:
      replicas: 1
      selector: { matchLabels: { app: rating } }
      template:
        metadata: { labels: { app: rating, env: dev } }
        spec:
          containers:
            - name: rating
              image: khanhnv2281/yas:rating-latest
              ports: [{ containerPort: 8089 }]
  - apiVersion: v1
    kind: Service
    metadata: { name: rating, namespace: yas-dev, labels: { app: rating, env: dev } }
    spec:
      selector: { app: rating }
      ports: [{ name: http, port: 80, targetPort: 8089 }]
